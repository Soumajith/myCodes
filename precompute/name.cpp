#include <bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cin>>s;
    int t;
    cin>>t;
    while(t--){
        string a;
        cin>>a;

        sort(a.begin(),a.end());
        sort(s.begin(),s.end());
        for (int i = 0; i < s.length(); i++)
        {
            while(a)
        }
        
        cout<<s<<endl;
        if(a == s){
            cout<<"Yes"<<endl;
        }
        else{
            cout<<"No"<<endl;
        }
        //incomplete
    }
}